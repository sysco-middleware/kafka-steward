package no.sysco.middleware.kafka.event.collector.cluster

import akka.actor.{ ActorRef, ActorSystem }

class ClusterEventCollector(implicit actorSystem: ActorSystem) {

  //  val clusterManager: ActorRef = actorSystem.actorOf(ClusterManager.props())
}
